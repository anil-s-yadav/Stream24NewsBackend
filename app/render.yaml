services:
  - type: web
    name: stream24news-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GOOGLE_CREDENTIALS
        value: 
    plan: free  # or standard, depending on your needs
